# Refactoring Examples

Each example is graph-first and MCP-first.

| Repository | Questions | Expected workflow | Capabilities invoked | Expected evidence | Expected answer |
| --- | --- | --- | --- | --- | --- |
| Ovok Core | Trace the login flow through the NestJS auth controller. | Follow shared workflow, run `EvidencePack` first, then `ImpactAnalysis` then `FindDependencies` then `FindDependents` then `FindCycles`, inspect files only if graph evidence is missing. | `EvidencePack`, `ImpactAnalysis`, `FindDependencies`, `FindDependents`, `FindCycles` | controller, route, service, dependency-injection edges; graph hash; confidence. | A refactoring plan with blast radius, dependency order, validation steps, and confidence. |
| Ghost | Explain the package entrypoint and public API surface. | Follow shared workflow, run `EvidencePack` first, then `ImpactAnalysis` then `FindDependencies` then `FindDependents` then `FindCycles`, inspect files only if graph evidence is missing. | `EvidencePack`, `ImpactAnalysis`, `FindDependencies`, `FindDependents`, `FindCycles` | package, exports, functions, interfaces; graph hash; confidence. | A refactoring plan with blast radius, dependency order, validation steps, and confidence. |
| durable-local | Find storage APIs and consumers before a refactor. | Follow shared workflow, run `EvidencePack` first, then `ImpactAnalysis` then `FindDependencies` then `FindDependents` then `FindCycles`, inspect files only if graph evidence is missing. | `EvidencePack`, `ImpactAnalysis`, `FindDependencies`, `FindDependents`, `FindCycles` | functions, interfaces, dependents, configuration; graph hash; confidence. | A refactoring plan with blast radius, dependency order, validation steps, and confidence. |
| 0xsarwagya | Summarize workspace packages and package dependencies. | Follow shared workflow, run `EvidencePack` first, then `ImpactAnalysis` then `FindDependencies` then `FindDependents` then `FindCycles`, inspect files only if graph evidence is missing. | `EvidencePack`, `ImpactAnalysis`, `FindDependencies`, `FindDependents`, `FindCycles` | workspace, packages, framework nodes, dependency edges; graph hash; confidence. | A refactoring plan with blast radius, dependency order, validation steps, and confidence. |
| Innosphere | Identify application modules and request ownership. | Follow shared workflow, run `EvidencePack` first, then `ImpactAnalysis` then `FindDependencies` then `FindDependents` then `FindCycles`, inspect files only if graph evidence is missing. | `EvidencePack`, `ImpactAnalysis`, `FindDependencies`, `FindDependents`, `FindCycles` | controllers, modules, routes, services; graph hash; confidence. | A refactoring plan with blast radius, dependency order, validation steps, and confidence. |
