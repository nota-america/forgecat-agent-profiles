# Configuration Analysis Examples

Each example is graph-first and MCP-first.

| Repository | Questions | Expected workflow | Capabilities invoked | Expected evidence | Expected answer |
| --- | --- | --- | --- | --- | --- |
| Ovok Core | Trace the login flow through the NestJS auth controller. | Follow shared workflow, run `EvidencePack` first, then `FindConfiguration` then `FindConfigurationUsage` then `FindDependencies` then `GraphStatistics`, inspect files only if graph evidence is missing. | `EvidencePack`, `FindConfiguration`, `FindConfigurationUsage`, `FindDependencies`, `GraphStatistics` | controller, route, service, dependency-injection edges; graph hash; confidence. | A configuration inventory with usage edges, unknowns, source evidence, and confidence. |
| Ghost | Explain the package entrypoint and public API surface. | Follow shared workflow, run `EvidencePack` first, then `FindConfiguration` then `FindConfigurationUsage` then `FindDependencies` then `GraphStatistics`, inspect files only if graph evidence is missing. | `EvidencePack`, `FindConfiguration`, `FindConfigurationUsage`, `FindDependencies`, `GraphStatistics` | package, exports, functions, interfaces; graph hash; confidence. | A configuration inventory with usage edges, unknowns, source evidence, and confidence. |
| durable-local | Find storage APIs and consumers before a refactor. | Follow shared workflow, run `EvidencePack` first, then `FindConfiguration` then `FindConfigurationUsage` then `FindDependencies` then `GraphStatistics`, inspect files only if graph evidence is missing. | `EvidencePack`, `FindConfiguration`, `FindConfigurationUsage`, `FindDependencies`, `GraphStatistics` | functions, interfaces, dependents, configuration; graph hash; confidence. | A configuration inventory with usage edges, unknowns, source evidence, and confidence. |
| 0xsarwagya | Summarize workspace packages and package dependencies. | Follow shared workflow, run `EvidencePack` first, then `FindConfiguration` then `FindConfigurationUsage` then `FindDependencies` then `GraphStatistics`, inspect files only if graph evidence is missing. | `EvidencePack`, `FindConfiguration`, `FindConfigurationUsage`, `FindDependencies`, `GraphStatistics` | workspace, packages, framework nodes, dependency edges; graph hash; confidence. | A configuration inventory with usage edges, unknowns, source evidence, and confidence. |
| Innosphere | Identify application modules and request ownership. | Follow shared workflow, run `EvidencePack` first, then `FindConfiguration` then `FindConfigurationUsage` then `FindDependencies` then `GraphStatistics`, inspect files only if graph evidence is missing. | `EvidencePack`, `FindConfiguration`, `FindConfigurationUsage`, `FindDependencies`, `GraphStatistics` | controllers, modules, routes, services; graph hash; confidence. | A configuration inventory with usage edges, unknowns, source evidence, and confidence. |
