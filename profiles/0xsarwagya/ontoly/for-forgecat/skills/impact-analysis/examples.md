# Impact Analysis Examples

Each example is graph-first and MCP-first.

| Repository | Questions | Expected workflow | Capabilities invoked | Expected evidence | Expected answer |
| --- | --- | --- | --- | --- | --- |
| Ovok Core | Trace the login flow through the NestJS auth controller. | Follow shared workflow, run `EvidencePack` first, then `ImpactAnalysis` then `FindDependents` then `FindDependencies` then `FindNode`, inspect files only if graph evidence is missing. | `EvidencePack`, `ImpactAnalysis`, `FindDependents`, `FindDependencies`, `FindNode` | controller, route, service, dependency-injection edges; graph hash; confidence. | A ranked blast-radius report with direct and transitive dependents, evidence edges, and confidence. |
| Ghost | Explain the package entrypoint and public API surface. | Follow shared workflow, run `EvidencePack` first, then `ImpactAnalysis` then `FindDependents` then `FindDependencies` then `FindNode`, inspect files only if graph evidence is missing. | `EvidencePack`, `ImpactAnalysis`, `FindDependents`, `FindDependencies`, `FindNode` | package, exports, functions, interfaces; graph hash; confidence. | A ranked blast-radius report with direct and transitive dependents, evidence edges, and confidence. |
| durable-local | Find storage APIs and consumers before a refactor. | Follow shared workflow, run `EvidencePack` first, then `ImpactAnalysis` then `FindDependents` then `FindDependencies` then `FindNode`, inspect files only if graph evidence is missing. | `EvidencePack`, `ImpactAnalysis`, `FindDependents`, `FindDependencies`, `FindNode` | functions, interfaces, dependents, configuration; graph hash; confidence. | A ranked blast-radius report with direct and transitive dependents, evidence edges, and confidence. |
| 0xsarwagya | Summarize workspace packages and package dependencies. | Follow shared workflow, run `EvidencePack` first, then `ImpactAnalysis` then `FindDependents` then `FindDependencies` then `FindNode`, inspect files only if graph evidence is missing. | `EvidencePack`, `ImpactAnalysis`, `FindDependents`, `FindDependencies`, `FindNode` | workspace, packages, framework nodes, dependency edges; graph hash; confidence. | A ranked blast-radius report with direct and transitive dependents, evidence edges, and confidence. |
| Innosphere | Identify application modules and request ownership. | Follow shared workflow, run `EvidencePack` first, then `ImpactAnalysis` then `FindDependents` then `FindDependencies` then `FindNode`, inspect files only if graph evidence is missing. | `EvidencePack`, `ImpactAnalysis`, `FindDependents`, `FindDependencies`, `FindNode` | controllers, modules, routes, services; graph hash; confidence. | A ranked blast-radius report with direct and transitive dependents, evidence edges, and confidence. |
