# Request Tracing Examples

Each example is graph-first and MCP-first.

| Repository | Questions | Expected workflow | Capabilities invoked | Expected evidence | Expected answer |
| --- | --- | --- | --- | --- | --- |
| Ovok Core | Trace the login flow through the NestJS auth controller. | Follow shared workflow, run `EvidencePack` first, then `TraceRequestLifecycle` then `FindResponsibleFunction` then `TraceExecution` then `FindNode`, inspect files only if graph evidence is missing. | `EvidencePack`, `TraceRequestLifecycle`, `FindResponsibleFunction`, `TraceExecution`, `FindNode` | controller, route, service, dependency-injection edges; graph hash; confidence. | A route-to-handler-to-callee walkthrough with source spans, relationship edges, and confidence. |
| Ghost | Explain the package entrypoint and public API surface. | Follow shared workflow, run `EvidencePack` first, then `TraceRequestLifecycle` then `FindResponsibleFunction` then `TraceExecution` then `FindNode`, inspect files only if graph evidence is missing. | `EvidencePack`, `TraceRequestLifecycle`, `FindResponsibleFunction`, `TraceExecution`, `FindNode` | package, exports, functions, interfaces; graph hash; confidence. | A route-to-handler-to-callee walkthrough with source spans, relationship edges, and confidence. |
| durable-local | Find storage APIs and consumers before a refactor. | Follow shared workflow, run `EvidencePack` first, then `TraceRequestLifecycle` then `FindResponsibleFunction` then `TraceExecution` then `FindNode`, inspect files only if graph evidence is missing. | `EvidencePack`, `TraceRequestLifecycle`, `FindResponsibleFunction`, `TraceExecution`, `FindNode` | functions, interfaces, dependents, configuration; graph hash; confidence. | A route-to-handler-to-callee walkthrough with source spans, relationship edges, and confidence. |
| 0xsarwagya | Summarize workspace packages and package dependencies. | Follow shared workflow, run `EvidencePack` first, then `TraceRequestLifecycle` then `FindResponsibleFunction` then `TraceExecution` then `FindNode`, inspect files only if graph evidence is missing. | `EvidencePack`, `TraceRequestLifecycle`, `FindResponsibleFunction`, `TraceExecution`, `FindNode` | workspace, packages, framework nodes, dependency edges; graph hash; confidence. | A route-to-handler-to-callee walkthrough with source spans, relationship edges, and confidence. |
| Innosphere | Identify application modules and request ownership. | Follow shared workflow, run `EvidencePack` first, then `TraceRequestLifecycle` then `FindResponsibleFunction` then `TraceExecution` then `FindNode`, inspect files only if graph evidence is missing. | `EvidencePack`, `TraceRequestLifecycle`, `FindResponsibleFunction`, `TraceExecution`, `FindNode` | controllers, modules, routes, services; graph hash; confidence. | A route-to-handler-to-callee walkthrough with source spans, relationship edges, and confidence. |
