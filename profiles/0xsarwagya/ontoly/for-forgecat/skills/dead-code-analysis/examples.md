# Dead Code Analysis Examples

Each example is graph-first and MCP-first.

| Repository | Questions | Expected workflow | Capabilities invoked | Expected evidence | Expected answer |
| --- | --- | --- | --- | --- | --- |
| Ovok Core | Trace the login flow through the NestJS auth controller. | Follow shared workflow, run `EvidencePack` first, then `FindDeadCode` then `FindUnusedFeature` then `FindEntrypoints` then `FindDependents`, inspect files only if graph evidence is missing. | `EvidencePack`, `FindDeadCode`, `FindUnusedFeature`, `FindEntrypoints`, `FindDependents` | controller, route, service, dependency-injection edges; graph hash; confidence. | A cautious dead-code report with candidates, false-positive checks, evidence, and confidence. |
| Ghost | Explain the package entrypoint and public API surface. | Follow shared workflow, run `EvidencePack` first, then `FindDeadCode` then `FindUnusedFeature` then `FindEntrypoints` then `FindDependents`, inspect files only if graph evidence is missing. | `EvidencePack`, `FindDeadCode`, `FindUnusedFeature`, `FindEntrypoints`, `FindDependents` | package, exports, functions, interfaces; graph hash; confidence. | A cautious dead-code report with candidates, false-positive checks, evidence, and confidence. |
| durable-local | Find storage APIs and consumers before a refactor. | Follow shared workflow, run `EvidencePack` first, then `FindDeadCode` then `FindUnusedFeature` then `FindEntrypoints` then `FindDependents`, inspect files only if graph evidence is missing. | `EvidencePack`, `FindDeadCode`, `FindUnusedFeature`, `FindEntrypoints`, `FindDependents` | functions, interfaces, dependents, configuration; graph hash; confidence. | A cautious dead-code report with candidates, false-positive checks, evidence, and confidence. |
| 0xsarwagya | Summarize workspace packages and package dependencies. | Follow shared workflow, run `EvidencePack` first, then `FindDeadCode` then `FindUnusedFeature` then `FindEntrypoints` then `FindDependents`, inspect files only if graph evidence is missing. | `EvidencePack`, `FindDeadCode`, `FindUnusedFeature`, `FindEntrypoints`, `FindDependents` | workspace, packages, framework nodes, dependency edges; graph hash; confidence. | A cautious dead-code report with candidates, false-positive checks, evidence, and confidence. |
| Innosphere | Identify application modules and request ownership. | Follow shared workflow, run `EvidencePack` first, then `FindDeadCode` then `FindUnusedFeature` then `FindEntrypoints` then `FindDependents`, inspect files only if graph evidence is missing. | `EvidencePack`, `FindDeadCode`, `FindUnusedFeature`, `FindEntrypoints`, `FindDependents` | controllers, modules, routes, services; graph hash; confidence. | A cautious dead-code report with candidates, false-positive checks, evidence, and confidence. |
