name: Catalog check

on:
  pull_request:
    paths:
      - README.md
      - profiles/**
      - scripts/check-readme-catalog.rb
      - scripts/check-profile-artifacts.rb
      - scripts/check-license-evidence.rb
      - scripts/license-baseline.txt
      - .github/workflows/catalog-check.yml
  push:
    branches:
      - main
    paths:
      - README.md
      - profiles/**
      - scripts/check-readme-catalog.rb
      - scripts/check-profile-artifacts.rb
      - scripts/check-license-evidence.rb
      - scripts/license-baseline.txt
      - .github/workflows/catalog-check.yml

jobs:
  readme-catalog:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"

      - name: Check README catalog
        run: ruby scripts/check-readme-catalog.rb

      - name: Fetch base branch
        if: github.event_name == 'pull_request'
        run: git fetch origin ${{ github.base_ref }} --depth=1

      - name: Check changed profile artifacts
        if: github.event_name == 'pull_request'
        run: ruby scripts/check-profile-artifacts.rb --changed-only origin/${{ github.base_ref }}..HEAD

      - name: Check license evidence on changed profiles
        if: github.event_name == 'pull_request'
        run: ruby scripts/check-license-evidence.rb --changed-only origin/${{ github.base_ref }}..HEAD
